FROM golang:1.21-alpine AS builder

WORKDIR /app

COPY go.mod ./
COPY main.go ./

RUN CGO_ENABLED=0 GOOS=linux go build -o backup-ops-agent main.go

FROM alpine:3.18

WORKDIR /app
COPY --from=builder /app/backup-ops-agent .

EXPOSE 8080

CMD ["./backup-ops-agent"]
